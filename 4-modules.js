// CommonJS, every file is module (by default)
// Modules - Encapsulated Code (only share minimum)
const names = require('./3-names');
const sayHi = require('./5-utils');
const data  = require('./6-alternativ');
require('./7-minegranade') 


// console.log(data);
// sayHi('susan');
// sayHi(names.john);
// sayHi(names.peter);
